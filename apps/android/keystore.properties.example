# Copy this file to keystore.properties and keep the real file private.
# The storeFile path is relative to the project root.
storeFile=release/illumined-upload.jks
storePassword=replace-with-private-password
keyAlias=illumined-upload
keyPassword=replace-with-private-password

# Supply Play version values at build time, for example:
# ./gradlew bundleRelease -PILLUMINED_VERSION_CODE=2 -PILLUMINED_VERSION_NAME=0.2.0
